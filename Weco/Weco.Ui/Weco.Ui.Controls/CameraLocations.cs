
namespace Weco.Ui.Controls
{
   using System;

   public enum CameraLocations
   {
      none,

      crawlerLeft, // light only
      crawlerRight, // light only

      crawlerFront,
      crawlerRear,

      bulletLeft,
      bulletRight,
      bulletDown,
   }
}